[tool.poetry]
name = "src"
version = "0.1.0"
description = "Python API for Speed Runner"
authors = ["Taeho <taehokimbusines@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.109.0"
uvicorn = "^0.25.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[tool.poetry.scripts]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
